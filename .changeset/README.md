# Changesets

Run `pnpm changeset` for a user-facing package change. Select the affected packages, choose their
semantic version bumps, and describe the behavior change. Run `pnpm changeset --empty` for a pull
request that does not release a package.

The release workflow consumes these files into package versions and changelogs.
